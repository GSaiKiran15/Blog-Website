<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/style.css">
    <title>Daily Journal</title>
</head>

<body>
    <%- include("header") -%>
        <% for (var i=0; i<headings.length; i++){ %>
            <% console.log(headings[i],headings.length) %>
                <div class="pages">
                    <h2 class="jheading">
                        <%= headings[i] %>
                    </h2>
                    <h3 class="jrnl">
                        <%= journals[i] %>
                    </h3>
                    <% } %>
                </div>
                <div class="csubmit">
                    <a href="/compose"> <button id="compose_button">Compose</button></a>
                </div>

                <%- include("footer") -%>

</body>


</html>